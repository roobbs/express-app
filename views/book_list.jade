extends layout

block content
  h1= title
  if book_list.length
    ul.displayBooks
      each book in book_list
        li.bookItem
          a(href=book.url) #{book.title}
          |  (#{book.author.name})
          

  else
    p There are no books.
